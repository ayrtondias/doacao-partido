import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'doacao-partido',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
